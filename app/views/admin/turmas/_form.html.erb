<%= form_with(model: [:admin, turma], local: true) do |f| %>

  <div class="card shadow-sm p-4">

    <div class="mb-3">
      <%= f.label :nome, "Nome da Turma", class: "form-label fw-semibold" %>
      <%= f.text_field :nome, class: "form-control", required: true %>
    </div>

    <div class="mb-3">
      <%= f.label :periodo, "Período", class: "form-label fw-semibold" %>
      <%= f.text_field :periodo, class: "form-control", placeholder: "Ex: 2024/1", required: true %>
    </div>

    <div class="mb-3">
      <%= f.label :descricao, "Descrição", class: "form-label fw-semibold" %>
      <%= f.text_area :descricao, rows: 3, class: "form-control" %>
    </div>

    <div class="d-flex justify-content-end">
      <%= f.submit class: "btn btn-primary px-4" %>
    </div>

  </div>

<% end %>